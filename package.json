{
  "name": "web-server",
  "version": "1.0.0",
  "description": "example express web server",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julio Chirinos",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
